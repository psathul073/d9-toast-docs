import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { ToastButton } from "@site/src/components/ToastButton";

## Top

<Tabs>
  
<TabItem value="top-right" label="Top Right" default>
```jsx
showToast("Top right toast", { position: "top-right" });
```
Click the button below to trigger a toast:

<ToastButton type="success" message="Top right toast" position="top-right" />

</TabItem>

<TabItem value="top-left" label="Top Left">
```jsx
showToast("Top left toast", { position: "top-left" });
```
Click the button below to trigger a toast:

<ToastButton
  type="success"
  message="Top left toast"
  position="top-left"
/>
</TabItem>

</Tabs>

---

## Bottom

<Tabs>
<TabItem value="bottom-right" label="Bottom Right" default>
```jsx
showToast("Bottom right toast",{ position: "bottom-right" });
```
Click the button below to trigger a toast:

<ToastButton
  type="success"
  message="Bottom right toast"
  position="bottom-right"
/>

</TabItem>

<TabItem value="bottom-left" label="Bottom Left">
```jsx
showToast("Bottom left toast",{ position: "bottom-left" });
```
Click the button below to trigger a toast:

<ToastButton
  type="success"
  message="Bottom left toast"
  position="bottom-left"
/>
  </TabItem>

</Tabs>

---

## Center

<Tabs>
  
<TabItem value="center" label="Center" default>
```jsx
showToast("Centered notification",{ position: "center" });
```
Click the button below to trigger a toast:

<ToastButton type="success" message="Centered notification" position="center" />

</TabItem>

<TabItem value="center-top" label="Center Top">

```jsx
showToast("Center top toast",{ position: "center-top" });
```
Click the button below to trigger a toast:

<ToastButton
  type="success"
  message="Center top toast"
  position="center-top"
/>
  </TabItem>

<TabItem value="center-bottom" label="Center Bottom">

```jsx
showToast("Center bottom toast",{ position: "center-bottom"  });
```
Click the button below to trigger a toast:

<ToastButton
  type="success"
  message="Center bottom toast"
  position="center-bottom"
/>
  </TabItem>

</Tabs>
