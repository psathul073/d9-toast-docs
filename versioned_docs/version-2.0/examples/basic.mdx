import { ToastButton } from "@site/src/components/ToastButton";

This page shows common real-world use cases for D9 Toast.

## Simple

```jsx
toast.success("Simple toast ...", {
  title: false,
});
```

Click the button below to trigger a toast:

 <ToastButton type="success" message="Simple toast ...." position="top-right" title={false} />

## Default

```jsx
toast("Default toast ...");
```

Click the button below to trigger a toast:

 <ToastButton type="default" message="Default toast ...."  />

## Success

```jsx
toast.success("Profile updated successfully");
```

Click the button below to trigger a toast:

<ToastButton type="success" message="Profile updated successfully" position="top-right" />

## Error

```jsx
toast.error("Something went wrong");
```

Click the button below to trigger a toast:

<ToastButton type="error" message="Something went wrong" position="top-right" />

## Info

```jsx
toast.info("New update available");
```

Click the button below to trigger a toast:

<ToastButton type="info" message="New update available" position="top-right" />

## Warning

```jsx
toast.warning("Invalid user data");
```

Click the button below to trigger a toast:

<ToastButton type="warning" message="Invalid user data" position="top-right" />
